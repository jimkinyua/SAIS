@model IEnumerable<SAIS.Models.SocialAssistanceProgram>

@{
    ViewData["Title"] = "Social Assistance Programs";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Social Assistance Programs</h2>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> New Program
    </a>
</div>

<div class="card">
    <div class="card-body">
        @if (Model.Any())
        {
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th>Program Name</th>
                                <th>Applications</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                    <tr>
                                        <td>
                                            <strong>@Html.DisplayFor(modelItem => item.ProgramName)</strong>
                                        </td>
                                        <td>
                                            <span class="badge bg-info">@item.AppliedPrograms.Count</span>
                                        </td>
                                        <td>
                                            <div class="btn-group" role="group">
                                                <a asp-action="Details" asp-route-id="@item.ProgramId" 
                                                   class="btn btn-sm btn-outline-info" title="View Details">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a asp-action="Edit" asp-route-id="@item.ProgramId" 
                                                   class="btn btn-sm btn-outline-warning" title="Edit">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@item.ProgramId" 
                                                   class="btn btn-sm btn-outline-danger" title="Delete">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                            }
                        </tbody>
                    </table>
                </div>
        }
        else
        {
                <div class="text-center py-5">
                    <i class="fas fa-hand-holding-heart fa-3x text-muted mb-3"></i>
                    <h5 class="text-muted">No programs found</h5>
                    <p class="text-muted">Start by adding a new social assistance program.</p>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="fas fa-plus"></i> Add First Program
                    </a>
                </div>
        }
    </div>
</div>
